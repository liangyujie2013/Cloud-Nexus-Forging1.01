# CNF 企业级整改清单（P1–P22）唯一可信来源

> 产品经理逐屏反馈的整改指令。本文件为唯一可信清单，逐条实现并勾选，杜绝遗漏/返工。
> 对标 VMware vCenter / Proxmox VE / SmartX CloudTower。

## 阶段一 · 紧急修复按钮假死（P1+P8+P9+P12+P15+P22）
- [ ] **P1/P22 退出登录**：清 Token + 跳登录页（app.js 用户菜单"退出登录"无 @click）
- [ ] **P8 模板"新建模板"**：弹窗表单 + 提交 + 反馈（view-compute.js 无 @click，后端缺 POST /vm-templates）
- [ ] **P8 模板"部署"**：从模板创建 VM（无 @click）
- [ ] **P9 ISO"上传ISO"**：本地上传 / URL 下载弹窗（view-compute.js 无 @click，后端缺 POST /iso-images）
- [ ] **P12 备份"新建备份任务"**：备份策略弹窗（view-availability.js 无 @click，后端缺 POST /backup-jobs）
- [x] **P15 网络"创建虚拟交换机"**：已有 @click="openCreate"（早期已修，复核通过）
- [x] **资源池"新建资源池"**：已有 @click="openPoolWiz"（复核通过）

## 阶段二 · 核心业务模块重构
- [ ] **P2 主机管理专业化**：列表增强(OS自动识别/物理逻辑核/VM数) + 详情页4 Tab(基本/硬件/运行VM/性能) + 添加主机3步向导
- [ ] **P4+P5 创建 VM 向导专业化**：GuestOS 智能选择(自动机器类型/固件) + CPU 默认只填 vCPU 数/高级模式 + NUMA 改节点选择(废弃128格子)
- [ ] **P6+P7 VM 列表与操作专业化**：列重设计(OS图标/状态色/负载条/IP说明qemu-guest-agent) + 右键菜单状态联动
- [ ] **P10 HA 企业级**：心跳5s×3失败判故障 + 防脑裂 + 准入控制(保留N台/X%) + VM优先级 + 容量评估
- [ ] **P11 迁移中心重设计**：右键主入口 + 迁移中心辅入口 + 4步向导(选源→选目标兼容检查→选项计算/存储→确认)
- [ ] **P13 存储管理关系梳理**：层级说明 + 存储池创建(类型→参数→挂载主机→测试) + 详情页(卷管理) + 存储迁移
- [ ] **P14 快照专业化**：磁盘快照(COW)/内存快照定义 + 快照树可视化 + 创建/恢复/删除/合并

## 阶段三 · UI/UX 专业化
- [ ] **P15+P16 网络可视化重构**：层级关系 + 创建交换机3步 + 拓扑 SVG/Canvas 分层(主机→交换机→VLAN→VM)
- [ ] **P17+P21 监控图表专业化**：图表标题带监控对象 + 图例/单位/Y轴自适应 + 仪表板重设计(KPI/集群对比柱状/TopN)
- [ ] **P18+P20 系统管理优化**：用户管理精简(必填用户名/密码/角色，其余折叠高级) + 关于页改版(产品/版本/授权，移除技术栈)
- [x] **P24 License 节点数**：社区 1-3 / 标准 4-31 / 企业 32-64（已完成 commit ed16066）

## 阶段四 · 模板/备份企业级（P8+P9+P12 深化）
- [ ] **模板管理**：从停机VM转换/新建空白 + 字段(名称/GuestOS/规格/系统盘/标签) + 批量部署
- [ ] **ISO 管理**：本地上传/URL下载 + 信息(名称/类型/大小/池/进度/MD5) + 创建VM选ISO
- [ ] **备份策略**：对象(单VM/多VM/集群) + 模式(全量/增量/差异) + 位置(本地/NFS/S3) + 调度(手动/Cron) + 保留(N份/N天)

---
**进度**：P24 ✅ | P15/资源池 复核✅ | 其余进行中
