{
  "name": "cnf",
  "version": "1.0.1",
  "description": "Cloud Nexus Forging (CNF) — 企业级 KVM 虚拟化管理平台",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:node": "tsx src/server-node.ts",
    "build": "vite build",
    "preview": "wrangler pages dev",
    "deploy": "npm run build && wrangler pages deploy",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "hono": "^4.12.26"
  },
  "devDependencies": {
    "@hono/node-server": "^2.0.6",
    "@hono/vite-build": "^1.2.0",
    "@hono/vite-dev-server": "^0.25.2",
    "tsx": "^4.22.4",
    "vite": "^6.3.5",
    "wrangler": "^4.4.0"
  }
}
